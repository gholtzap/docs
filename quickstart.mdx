---
title: 'Quickstart'
icon: 'rocket'
---

# Quickstart

Welcome to the quickstart guide for **Solisa Calendar**. This guide will walk you through the setup and basic integration of our calendar components into your React project.

## Prerequisites

Before you begin, ensure that you have:
- A working React environment (e.g., using [Next.js](https://nextjs.org/) or Create React App).
- Node.js installed on your machine.

## Installation

Install the `solisa-calendar` package using your preferred package manager. For example:

```bash
npm install solisa-calendar
# or
yarn add solisa-calendar
```
## Basic Usage
Here’s an example demonstrating how to render the unified calendar view (FullCal) with sample events:
```jsx
'use client'

import React from 'react';
import { FullCal } from 'solisa-calendar';
import { events } from '@/app/temp/sampledata';

const App = () => {
  return (
    <FullCal availableViews={['week', 'month']} events={events} defaultView="week" />
  );
};

export default App;
```

Sample data not provided. Add your own events by creating an <Link href="/types/Event">Event</Link>, or array of <Link href="/types/Event">Events</Link>.